---
- name: Getting Started First Playbook Extended
  hosts: localhost
  gather_facts: true
  become: yes
  tasks:

    - name: Create local folder for downloads
      file:
        path: /opt/test
        state: directory
        mode: 0777

    - name: print to file
      ansible.builtin.shell:
        cmd: echo "hello" > ~/test/hello.txt
           
       
